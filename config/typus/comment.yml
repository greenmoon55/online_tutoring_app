# Use the README file as a reference to customize settings.

Comment:
  fields:
    default: student_id, teacher_id, evaluation
    form: student_id, teacher_id, evaluation
  relationships: 
  application: Application
