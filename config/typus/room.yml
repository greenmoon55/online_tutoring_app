# Use the README file as a reference to customize settings.

Room:
  fields:
    default: outline, user_id
    form: outline, user_id
  relationships: room_student_relationships, students
  application: Application
