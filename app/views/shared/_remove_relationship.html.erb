<%= form_tag(relationship_path(user.id), method:'delete', :multipart => true) do%>
  <%= hidden_field_tag :user_id,user.id%>
  <%= submit_tag "解除关系", class: "btn btn-warning", data: {confirm: '你确定吗？ '}%>
<% end %>
