<%= form_tag(comment_path(comment), method:"delete") do%>
<%= hidden_field_tag :student_id, comment.student_id%>
<%= hidden_field_tag :teacher_id, comment.teacher_id%>
<%= submit_tag "删除", class: "btn btn-danger", id:"comment_del"%>
<% end %>
