<%= subscribe_to "/messages/#{current_user.id}" if signed_in?%>
<script>
  PrivatePub.subscribe("/messages/<%= current_user.id %>", function(data, channel) {
    $("#chat").append(data.message.content);
    console.log(data);
  });
  (function poll(){
     setTimeout(function(){
        $.ajax({ url: "http://localhost:3000/refresh", success: function(data){
          //Update your dashboard gauge

          //Setup the next poll recursively
          poll();
        }});
    }, 30000);
  })();  
</script>
<%= online_users %>
