

  <%= form_for(user.requests.build(sender_id: current_user.id), url:request_path,:method=> :post) do |f| %>
  <div><%= f.text_field :content %></div>
  <%= f.submit "加为好友", class: "btn  btn-primary" %>
  <% end %>	

